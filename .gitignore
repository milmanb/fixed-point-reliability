__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Datasets are downloaded on demand
data/

# Large per-image dumps; regenerate them with scripts/evaluate_models.py
results/**/per_image*
*.ckpt
*.pt
# Trained models are published so the results can be checked without retraining
!checkpoints/*.pt

# LaTeX build products
*.aux
*.log
*.out
*.synctex.gz
*.fdb_latexmk
*.fls

# Local scratch comparisons
.cache/

# LaTeX build products in report/
report/main.pdf
*.bbl
*.blg
